'use client';

import styled from '@emotion/styled';
import { Heading } from '@chakra-ui/react';

export const HeaderMessage = styled(Heading)`
    padding: 56px 28px 0;
    font-weight: 400;
    letter-spacing: -0.02em;
    white-space: pre-line;
    text-align: left;
    align-self: flex-start;
`;
